--[[
    ╔══════════════════════════════════════════════╗
    ║             NEGRO HUB ULTRA                 ║
    ║                                              ║
    ║   Premium Intro + Loading + Animated Hub    ║
    ║   X = Chiudi | N = Riapri                   ║
    ╚══════════════════════════════════════════════╝
]]

local TweenService = game:GetService("TweenService")
local CoreGui = game:GetService("CoreGui")

--========================================================--
-- CONFIG
--========================================================--

local C = {
    Pink   = Color3.fromRGB(255, 0, 125),
    Purple = Color3.fromRGB(145, 0, 255),
    Blue   = Color3.fromRGB(0, 190, 255),
    Cyan   = Color3.fromRGB(0, 255, 220),
    Red    = Color3.fromRGB(255, 45, 65),

    Black  = Color3.fromRGB(2, 2, 7),
    Panel  = Color3.fromRGB(10, 10, 18),
    Panel2 = Color3.fromRGB(17, 17, 28),
    White  = Color3.fromRGB(245, 245, 255),
    Gray   = Color3.fromRGB(135, 135, 155)
}

local function makeTween(object, time, properties, style, direction)
    local info = TweenInfo.new(
        time or 0.5,
        style or Enum.EasingStyle.Quint,
        direction or Enum.EasingDirection.Out
    )

    local tween = TweenService:Create(object, info, properties)
    tween:Play()

    return tween
end

--========================================================--
-- CLEANUP
--========================================================--

pcall(function()
    local old = CoreGui:FindFirstChild("NegroHubUltra")
    if old then
        old:Destroy()
    end
end)

--========================================================--
-- MAIN GUI
--========================================================--

local gui = Instance.new("ScreenGui")
gui.Name = "NegroHubUltra"
gui.IgnoreGuiInset = true
gui.ResetOnSpawn = false
gui.DisplayOrder = 999999
gui.Parent = CoreGui

--========================================================--
-- INTRO GUI
-- SEPARATA DALL'HUB
--========================================================--

local introGui = Instance.new("Frame")
introGui.Name = "Intro"
introGui.Size = UDim2.fromScale(1, 1)
introGui.BackgroundColor3 = C.Black
introGui.BorderSizePixel = 0
introGui.ZIndex = 1
introGui.Parent = gui

-- Background gradient
local introGradient = Instance.new("UIGradient")
introGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(2, 2, 8)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(14, 3, 27)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(2, 7, 18))
})
introGradient.Rotation = 35
introGradient.Parent = introGui

--========================================================--
-- INTRO GLOW
--========================================================--

local introGlow = Instance.new("Frame")
introGlow.AnchorPoint = Vector2.new(0.5, 0.5)
introGlow.Position = UDim2.fromScale(0.5, 0.48)
introGlow.Size = UDim2.fromScale(0.65, 0.4)
introGlow.BackgroundColor3 = C.Purple
introGlow.BackgroundTransparency = 0.94
introGlow.BorderSizePixel = 0
introGlow.ZIndex = 2
introGlow.Parent = introGui

local introGlowCorner = Instance.new("UICorner")
introGlowCorner.CornerRadius = UDim.new(1, 0)
introGlowCorner.Parent = introGlow

--========================================================--
-- PARTICLES
--========================================================--

local particleFolder = Instance.new("Folder")
particleFolder.Parent = introGui

for i = 1, 55 do

    local particle = Instance.new("Frame")
    local size = math.random(1, 4)

    particle.Size = UDim2.fromOffset(size, size)
    particle.Position = UDim2.fromScale(
        math.random(),
        math.random()
    )

    particle.BackgroundColor3 = Color3.fromHSV(
        math.random(),
        0.8,
        1
    )

    particle.BackgroundTransparency =
        math.random(45, 90) / 100

    particle.BorderSizePixel = 0
    particle.ZIndex = 3
    particle.Parent = particleFolder

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(1, 0)
    corner.Parent = particle

    task.spawn(function()

        while particle.Parent do

            local target = UDim2.fromScale(
                math.random(),
                math.random()
            )

            local t = makeTween(
                particle,
                math.random(3, 8),
                {
                    Position = target,
                    BackgroundTransparency =
                        math.random(30, 90) / 100
                },
                Enum.EasingStyle.Sine,
                Enum.EasingDirection.InOut
            )

            t.Completed:Wait()
        end

    end)
end

--========================================================--
-- INTRO CONTAINER
--========================================================--

local introContainer = Instance.new("Frame")
introContainer.AnchorPoint = Vector2.new(0.5, 0.5)
introContainer.Position = UDim2.fromScale(0.5, 0.5)
introContainer.Size = UDim2.fromScale(0.78, 0.42)
introContainer.BackgroundTransparency = 1
introContainer.ZIndex = 10
introContainer.Parent = introGui

local introScale = Instance.new("UIScale")
introScale.Scale = 0.75
introScale.Parent = introContainer

--========================================================--
-- INTRO TITLE
--========================================================--

local introTitle = Instance.new("TextLabel")
introTitle.AnchorPoint = Vector2.new(0.5, 0)
introTitle.Position = UDim2.fromScale(0.5, 0.22)
introTitle.Size = UDim2.fromScale(0.95, 0.22)
introTitle.BackgroundTransparency = 1
introTitle.Text = "Negro di Fiducia presenta"
introTitle.TextColor3 = C.White
introTitle.TextScaled = true
introTitle.Font = Enum.Font.GothamBold
introTitle.TextTransparency = 1
introTitle.ZIndex = 15
introTitle.Parent = introContainer

local titleGradient = Instance.new("UIGradient")
titleGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, C.Pink),
    ColorSequenceKeypoint.new(0.3, C.Purple),
    ColorSequenceKeypoint.new(0.65, C.Blue),
    ColorSequenceKeypoint.new(1, C.Pink)
})
titleGradient.Parent = introTitle

--========================================================--
-- LOADING TEXT
--========================================================--

local loadingText = Instance.new("TextLabel")
loadingText.AnchorPoint = Vector2.new(0.5, 0)
loadingText.Position = UDim2.fromScale(0.5, 0.48)
loadingText.Size = UDim2.fromScale(0.85, 0.08)
loadingText.BackgroundTransparency = 1
loadingText.Text = "LOADING SCRIPT FOR THIS GAME"
loadingText.TextColor3 = C.Gray
loadingText.TextScaled = true
loadingText.Font = Enum.Font.GothamMedium
loadingText.TextTransparency = 1
loadingText.ZIndex = 15
loadingText.Parent = introContainer

--========================================================--
-- BAR
--========================================================--

local barOuter = Instance.new("Frame")
barOuter.AnchorPoint = Vector2.new(0.5, 0)
barOuter.Position = UDim2.fromScale(0.5, 0.60)
barOuter.Size = UDim2.fromScale(0.62, 0.045)
barOuter.BackgroundColor3 = Color3.fromRGB(25, 25, 38)
barOuter.BackgroundTransparency = 1
barOuter.BorderSizePixel = 0
barOuter.ZIndex = 15
barOuter.Parent = introContainer

local barCorner = Instance.new("UICorner")
barCorner.CornerRadius = UDim.new(1, 0)
barCorner.Parent = barOuter

local barStroke = Instance.new("UIStroke")
barStroke.Thickness = 1
barStroke.Transparency = 0.3
barStroke.Color = C.Purple
barStroke.Parent = barOuter

local progress = Instance.new("Frame")
progress.Size = UDim2.fromScale(0, 1)
progress.BackgroundColor3 = C.Purple
progress.BorderSizePixel = 0
progress.ZIndex = 16
progress.Parent = barOuter

local progressCorner = Instance.new("UICorner")
progressCorner.CornerRadius = UDim.new(1, 0)
progressCorner.Parent = progress

local progressGradient = Instance.new("UIGradient")
progressGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, C.Pink),
    ColorSequenceKeypoint.new(0.45, C.Purple),
    ColorSequenceKeypoint.new(1, C.Cyan)
})
progressGradient.Parent = progress

--========================================================--
-- SHINE
--========================================================--

local shine = Instance.new("Frame")
shine.Size = UDim2.fromScale(0.08, 2)
shine.Position = UDim2.fromScale(-0.1, -0.5)
shine.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
shine.BackgroundTransparency = 0.65
shine.BorderSizePixel = 0
shine.ZIndex = 17
shine.Parent = progress

local shineGradient = Instance.new("UIGradient")
shineGradient.Transparency = NumberSequence.new({
    NumberSequenceKeypoint.new(0, 1),
    NumberSequenceKeypoint.new(0.5, 0),
    NumberSequenceKeypoint.new(1, 1)
})
shineGradient.Parent = shine

--========================================================--
-- PERCENTAGE
--========================================================--

local percentage = Instance.new("TextLabel")
percentage.AnchorPoint = Vector2.new(0.5, 0)
percentage.Position = UDim2.fromScale(0.5, 0.68)
percentage.Size = UDim2.fromScale(0.25, 0.08)
percentage.BackgroundTransparency = 1
percentage.Text = "0%"
percentage.TextColor3 = C.White
percentage.TextScaled = true
percentage.Font = Enum.Font.GothamBold
percentage.TextTransparency = 1
percentage.ZIndex = 15
percentage.Parent = introContainer

--========================================================--
-- INTRO RGB
--========================================================--

task.spawn(function()

    while introGui.Parent do

        local t = os.clock()

        titleGradient.Offset =
            Vector2.new(math.sin(t * 0.8) * 0.5, 0)

        progressGradient.Offset =
            Vector2.new(math.sin(t * 1.4) * 0.5, 0)

        introGlow.BackgroundColor3 =
            Color3.fromHSV(
                (t * 0.08) % 1,
                0.9,
                1
            )

        barStroke.Color =
            Color3.fromHSV(
                (t * 0.12) % 1,
                0.8,
                1
            )

        task.wait()
    end

end)

--========================================================--
-- INTRO IN
--========================================================--

makeTween(
    introScale,
    1.2,
    {Scale = 1}
)

makeTween(
    introTitle,
    1,
    {TextTransparency = 0}
)

makeTween(
    loadingText,
    1,
    {TextTransparency = 0}
)

makeTween(
    barOuter,
    1,
    {BackgroundTransparency = 0.15}
)

makeTween(
    percentage,
    1,
    {TextTransparency = 0}
)

--========================================================--
-- LOADING
--========================================================--

for i = 0, 100 do

    percentage.Text = i .. "%"

    makeTween(
        progress,
        0.08,
        {
            Size = UDim2.fromScale(i / 100, 1)
        },
        Enum.EasingStyle.Linear
    )

    task.wait(0.035)
end

percentage.Text = "100%"

task.wait(0.7)

--========================================================--
-- INTRO OUT
--========================================================--

for _, object in ipairs({
    introTitle,
    loadingText,
    percentage
}) do

    makeTween(
        object,
        0.55,
        {
            TextTransparency = 1
        }
    )

end

makeTween(
    barOuter,
    0.55,
    {
        BackgroundTransparency = 1
    }
)

makeTween(
    introGlow,
    0.55,
    {
        BackgroundTransparency = 1
    }
)

makeTween(
    introContainer,
    0.65,
    {
        Position = UDim2.fromScale(0.5, 0.46)
    }
)

task.wait(0.65)

--========================================================--
-- IMPORTANT FIX
-- L'INTRO VIENE DISTRUTTA COMPLETAMENTE
--========================================================--

introGui:Destroy()

--========================================================--
-- HUB LAYER
--========================================================--

local hubLayer = Instance.new("Frame")
hubLayer.Name = "HubLayer"
hubLayer.Size = UDim2.fromScale(1, 1)
hubLayer.BackgroundTransparency = 1
hubLayer.BorderSizePixel = 0
hubLayer.ZIndex = 100
hubLayer.Parent = gui

--========================================================--
-- HUB
--========================================================--

local hub = Instance.new("Frame")
hub.Name = "MainHub"
hub.AnchorPoint = Vector2.new(0.5, 0.5)
hub.Position = UDim2.fromScale(0.5, 0.5)
hub.Size = UDim2.fromScale(0.42, 0.38)
hub.BackgroundColor3 = C.Panel
hub.BackgroundTransparency = 1
hub.BorderSizePixel = 0
hub.ZIndex = 110
hub.Parent = hubLayer

local hubCorner = Instance.new("UICorner")
hubCorner.CornerRadius = UDim.new(0, 20)
hubCorner.Parent = hub

local hubStroke = Instance.new("UIStroke")
hubStroke.Thickness = 2
hubStroke.Transparency = 0.1
hubStroke.Parent = hub

local hubGradient = Instance.new("UIGradient")
hubGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(21, 8, 34)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(8, 12, 25)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(20, 5, 25))
})
hubGradient.Rotation = 45
hubGradient.Parent = hub

--========================================================--
-- HEADER
--========================================================--

local header = Instance.new("Frame")
header.Position = UDim2.fromScale(0.035, 0.035)
header.Size = UDim2.fromScale(0.93, 0.18)
header.BackgroundColor3 = C.Panel2
header.BackgroundTransparency = 0.15
header.BorderSizePixel = 0
header.ZIndex = 115
header.Parent = hub

local headerCorner = Instance.new("UICorner")
headerCorner.CornerRadius = UDim.new(0, 14)
headerCorner.Parent = header

--========================================================--
-- TITLE
--========================================================--

local hubTitle = Instance.new("TextLabel")
hubTitle.Position = UDim2.fromScale(0.05, 0.10)
hubTitle.Size = UDim2.fromScale(0.68, 0.52)
hubTitle.BackgroundTransparency = 1
hubTitle.Text = "NEGRO HUB"
hubTitle.TextColor3 = C.White
hubTitle.TextScaled = true
hubTitle.Font = Enum.Font.GothamBlack
hubTitle.TextXAlignment = Enum.TextXAlignment.Left
hubTitle.ZIndex = 120
hubTitle.Parent = header

local hubTitleGradient = Instance.new("UIGradient")
hubTitleGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, C.Pink),
    ColorSequenceKeypoint.new(0.5, C.Purple),
    ColorSequenceKeypoint.new(1, C.Blue)
})
hubTitleGradient.Parent = hubTitle

local subtitle = Instance.new("TextLabel")
subtitle.Position = UDim2.fromScale(0.052, 0.62)
subtitle.Size = UDim2.fromScale(0.65, 0.24)
subtitle.BackgroundTransparency = 1
subtitle.Text = "Premium Script Hub"
subtitle.TextColor3 = C.Gray
subtitle.TextScaled = true
subtitle.Font = Enum.Font.GothamMedium
subtitle.TextXAlignment = Enum.TextXAlignment.Left
subtitle.ZIndex = 120
subtitle.Parent = header

--========================================================--
-- CLOSE X
--========================================================--

local closeButton = Instance.new("TextButton")
closeButton.AnchorPoint = Vector2.new(1, 0.5)
closeButton.Position = UDim2.fromScale(0.97, 0.5)
closeButton.Size = UDim2.fromScale(0.075, 0.58)
closeButton.BackgroundColor3 = C.Red
closeButton.BorderSizePixel = 0
closeButton.Text = "×"
closeButton.TextColor3 = C.White
closeButton.TextScaled = true
closeButton.Font = Enum.Font.GothamBold
closeButton.AutoButtonColor = false
closeButton.ZIndex = 125
closeButton.Parent = header

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(0, 10)
closeCorner.Parent = closeButton

local closeStroke = Instance.new("UIStroke")
closeStroke.Color = Color3.fromRGB(255, 110, 125)
closeStroke.Thickness = 1
closeStroke.Parent = closeButton

closeButton.MouseEnter:Connect(function()

    makeTween(
        closeButton,
        0.18,
        {
            BackgroundColor3 = Color3.fromRGB(255, 30, 55),
            Size = UDim2.fromScale(0.085, 0.65)
        }
    )

end)

closeButton.MouseLeave:Connect(function()

    makeTween(
        closeButton,
        0.18,
        {
            BackgroundColor3 = C.Red,
            Size = UDim2.fromScale(0.075, 0.58)
        }
    )

end)

--========================================================--
-- CONTENT
--========================================================--

local content = Instance.new("Frame")
content.Position = UDim2.fromScale(0.035, 0.245)
content.Size = UDim2.fromScale(0.93, 0.71)
content.BackgroundColor3 = Color3.fromRGB(5, 5, 11)
content.BackgroundTransparency = 0.15
content.BorderSizePixel = 0
content.ZIndex = 115
content.Parent = hub

local contentCorner = Instance.new("UICorner")
contentCorner.CornerRadius = UDim.new(0, 14)
contentCorner.Parent = content

local welcome = Instance.new("TextLabel")
welcome.Position = UDim2.fromScale(0.06, 0.12)
welcome.Size = UDim2.fromScale(0.88, 0.17)
welcome.BackgroundTransparency = 1
welcome.Text = "WELCOME"
welcome.TextColor3 = C.White
welcome.TextScaled = true
welcome.Font = Enum.Font.GothamBlack
welcome.ZIndex = 120
welcome.Parent = content

local welcomeGradient = Instance.new("UIGradient")
welcomeGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, C.Pink),
    ColorSequenceKeypoint.new(0.5, C.Purple),
    ColorSequenceKeypoint.new(1, C.Cyan)
})
welcomeGradient.Parent = welcome

local description = Instance.new("TextLabel")
description.Position = UDim2.fromScale(0.08, 0.34)
description.Size = UDim2.fromScale(0.84, 0.12)
description.BackgroundTransparency = 1
description.Text = "NEGRO HUB IS READY"
description.TextColor3 = C.Gray
description.TextScaled = true
description.Font = Enum.Font.GothamMedium
description.ZIndex = 120
description.Parent = content

--========================================================--
-- ONLINE STATUS
--========================================================--

local status = Instance.new("Frame")
status.AnchorPoint = Vector2.new(0.5, 0)
status.Position = UDim2.fromScale(0.5, 0.56)
status.Size = UDim2.fromScale(0.42, 0.13)
status.BackgroundColor3 = Color3.fromRGB(8, 38, 28)
status.BackgroundTransparency = 0.1
status.BorderSizePixel = 0
status.ZIndex = 120
status.Parent = content

local statusCorner = Instance.new("UICorner")
statusCorner.CornerRadius = UDim.new(1, 0)
statusCorner.Parent = status

local statusDot = Instance.new("Frame")
statusDot.AnchorPoint = Vector2.new(0, 0.5)
statusDot.Position = UDim2.fromScale(0.12, 0.5)
statusDot.Size = UDim2.fromScale(0.08, 0.3)
statusDot.BackgroundColor3 = Color3.fromRGB(0, 255, 150)
statusDot.BorderSizePixel = 0
statusDot.ZIndex = 125
statusDot.Parent = status

local dotCorner = Instance.new("UICorner")
dotCorner.CornerRadius = UDim.new(1, 0)
dotCorner.Parent = statusDot

local statusText = Instance.new("TextLabel")
statusText.Position = UDim2.fromScale(0.25, 0)
statusText.Size = UDim2.fromScale(0.65, 1)
statusText.BackgroundTransparency = 1
statusText.Text = "ONLINE"
statusText.TextColor3 = Color3.fromRGB(0, 255, 170)
statusText.TextScaled = true
statusText.Font = Enum.Font.GothamBold
statusText.ZIndex = 125
statusText.Parent = status

--========================================================--
-- REOPEN CIRCLE
--========================================================--

local reopen = Instance.new("TextButton")
reopen.Name = "ReopenButton"
reopen.AnchorPoint = Vector2.new(0.5, 0)
reopen.Position = UDim2.fromScale(0.5, 0.025)
reopen.Size = UDim2.fromOffset(58, 58)
reopen.BackgroundColor3 = Color3.fromRGB(9, 9, 17)
reopen.BorderSizePixel = 0
reopen.Text = "N"
reopen.TextColor3 = C.White
reopen.TextScaled = true
reopen.Font = Enum.Font.GothamBlack
reopen.AutoButtonColor = false
reopen.Visible = false
reopen.ZIndex = 300
reopen.Parent = gui

local reopenCorner = Instance.new("UICorner")
reopenCorner.CornerRadius = UDim.new(1, 0)
reopenCorner.Parent = reopen

local reopenStroke = Instance.new("UIStroke")
reopenStroke.Thickness = 2
reopenStroke.Parent = reopen

--========================================================--
-- RGB HUB
--========================================================--

task.spawn(function()

    while gui.Parent do

        local t = os.clock()

        hubStroke.Color =
            Color3.fromHSV(
                (t * 0.08) % 1,
                0.9,
                1
            )

        reopenStroke.Color =
            Color3.fromHSV(
                (t * 0.15) % 1,
                0.9,
                1
            )

        hubTitleGradient.Offset =
            Vector2.new(
                math.sin(t * 0.8) * 0.5,
                0
            )

        welcomeGradient.Offset =
            Vector2.new(
                math.sin(t * 0.9) * 0.5,
                0
            )

        task.wait()
    end

end)

--========================================================--
-- HUB OPEN ANIMATION
--========================================================--

makeTween(
    hub,
    0.8,
    {
        Size = UDim2.fromScale(0.58, 0.62),
        BackgroundTransparency = 0.08
    },
    Enum.EasingStyle.Back
)

--========================================================--
-- CLOSE HUB
--========================================================--

local function closeHub()

    local animation = makeTween(
        hub,
        0.55,
        {
            Size = UDim2.fromScale(0.42, 0.35),
            BackgroundTransparency = 1
        },
        Enum.EasingStyle.Back,
        Enum.EasingDirection.In
    )

    animation.Completed:Wait()

    hub.Visible = false

    reopen.Visible = true
    reopen.Size = UDim2.fromOffset(0, 0)

    makeTween(
        reopen,
        0.55,
        {
            Size = UDim2.fromOffset(58, 58)
        },
        Enum.EasingStyle.Back
    )
end

--========================================================--
-- OPEN HUB
--========================================================--

local function openHub()

    makeTween(
        reopen,
        0.3,
        {
            Size = UDim2.fromOffset(0, 0)
        },
        Enum.EasingStyle.Back,
        Enum.EasingDirection.In
    )

    task.wait(0.3)

    reopen.Visible = false
    hub.Visible = true

    hub.Size = UDim2.fromScale(0.42, 0.35)
    hub.BackgroundTransparency = 1

    makeTween(
        hub,
        0.7,
        {
            Size = UDim2.fromScale(0.58, 0.62),
            BackgroundTransparency = 0.08
        },
        Enum.EasingStyle.Back
    )
end

--========================================================--
-- BUTTON CONNECTIONS
--========================================================--

closeButton.MouseButton1Click:Connect(closeHub)
reopen.MouseButton1Click:Connect(openHub)

print("NEGRO HUB — 100% LOADED")
